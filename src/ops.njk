{% extends "base.njk" %}

{% block content %}

<header class="[ hero is-intro ] [ u-grid ]">
  <aside class="intro-left">
    <h1>Hi, I'm Kevin {% include "verified.njk" %}</h1>
    <h2 class="font-size-3">a Platform & DevOps Engineer working at <a href="https://aoe.com" title="Visit aoe.com">AOE GmbH</a>.</h2>
  </aside>
  <aside class="intro-right">
    <img src="/assets/img/avatar-aoe.jpg" class="avatar" alt="A photograph of me, Kevin Gimbel, in front of a grey background" />
  </aside>
<div class="intro-cta">
  <p class="font-size-2">I've spent the last <span class="is-highlighted bg-accent-3">15 years learning to build websites & run servers</span>; worked in e-Commerce from 2013 to 2023, and started focusing on <span class="is-highlighted bg-accent-3">Platform Engineering</span> in early 2023. Right now I'm part of a DevOps/Platform Team running and building a Kubernetes based Platform for multiple developer teams.</p>

  <p class="font-size--1 buzzes">🐝 Buzzwords: <a href="#cloud-computing" class="is-highlighted bg-accent-5">Cloud Computing</a> <a href="#kubernetes" class="is-highlighted bg-accent-6">Kubernetes</a> <a href="#automation" class="is-highlighted bg-accent-3">Automation</a> <a href="#monitoring" class="is-highlighted bg-accent-4">Monitoring</a></p>
</div>
</header>

  <header class="[ u-grid ]">
      <hgroup class="[ skill-section gc-full ]">
        <h3 class="font-size-5">Skills, tools, and other work stuff</h3>
      </hgroup>
  </header>

  {% for page in collections.sections %}
    <section id="{{ page.data.id }}" data-icon="{{ page.data.icon or "~" }}" class="[ bg-accent-{{ page.data.accent_id }} ] [ skill-section ]">
      {%- if page.data.sub_heading or page.data.title -%}
      <hgroup>
        {%- if page.data.title -%}
        <h3 class="font-size-5">{{ page.data.title }} {{ page.data.icon }}</h3>
        {%- endif -%}
        {%- if page.data.sub_heading -%}
        <h4 class="font-size-3">{{ page.data.sub_heading | safe }}</h4>
        {%- endif -%}
      </hgroup>
      {%- endif -%}
      <div class="content">
        {{ page.templateContent | safe }}
        {%- if page.data.skills.length -%}
        <ul class="[ d-grid ] [ c-skill-list ] list-accent" style="--grid-columns: 3;">
          {% for skill in page.data.skills %}
          <li class="[ c-skill-list__item ]">{{ skill | safe }}</li>
          {% endfor %}
        </ul>
        {%- endif -%}
      </div>
    </section>
  {%- endfor -%}
</section>

{% endblock %}
